# Experiment 9: 多 seed 复现

改动:

- 在当时的正式单图基线上加入显式 `seed` 控制。
- 跑 `seed in {2024, 2025, 2026}` 的 `300 epoch` 对比。

实验结论:

- 三组 `test_auc` 分别为:
  - `0.709478`
  - `0.707565`
  - `0.706154`
- 均值约 `0.7077`。
- 波动很小。

结论:

- 长训后的单图基线是稳定的，不是单次偶然值。
