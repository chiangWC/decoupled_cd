# Component Guidelines

> Not applicable until the project has a frontend.

---

## Current State

The repository has no UI component system. Do not invent component, prop, styling, or accessibility conventions for Python model and experiment work.

---

## Future Rule

If a frontend is added later, document conventions from the implemented UI code, not aspirational defaults. Include real component paths, props patterns, styling method, and accessibility expectations after they exist.
